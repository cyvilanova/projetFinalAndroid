<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/greenAccent</item>
        <item name="colorButtonNormal">@color/buttonBrown</item>
        <item name="toolbarStyle">@color/lightBeige</item>
        <item name="android:textColor">@color/darkBrown</item>
        <item name="android:editTextColor">@color/darkBrown</item>
        <item name="android:textColorPrimary">@color/darkBrown</item>
        <item name="android:textColorSecondary">@color/darkBrown</item>
        <item name="android:textColorTertiary">@color/darkBrown</item>
        <item name="android:textColorPrimaryInverse">@color/darkBrown</item>
        <item name="android:textColorSecondaryInverse">@color/darkBrown</item>
        <item name="android:textColorTertiaryInverse">@color/darkBrown</item>
    </style>

</resources>
